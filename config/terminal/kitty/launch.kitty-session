# Default kitty startup session.
# Per-project dev layouts are generated by the `devwin` shell function into
# ~/.local/state/devwin/ -- this file is just the plain startup window.
layout tall
cd ~
launch
